{% extends 'base.html.twig' %}

{% block title %}Ide{% endblock %}

{% block body %}
    <h1>Ide</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ide.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ide.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ide_index') }}">back to list</a>

    <a href="{{ path('app_ide_edit', {'id': ide.id}) }}">edit</a>

    {{ include('ide/_delete_form.html.twig') }}
{% endblock %}
