<form method="post" action="{{ path('app_ide_delete', {'id': ide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ide.id) }}">
    <button class="btn">Delete</button>
</form>
