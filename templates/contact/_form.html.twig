{{ form_start(form, {'attr': {'class': 'd-flex flex-column align-item-center'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
