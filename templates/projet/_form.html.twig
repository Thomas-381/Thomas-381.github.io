{{ form_start(form, {'attr': {'class': 'd-flex flex-column justify-content-center align-items-center w-75 gap-md-2'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
