{# barre de navigation  #}
<nav class="navbar navbar-expand-md fixed-top flex-row justify-content-center align-items-center bg-body-tertiary gap-md-3">
    <a class="navbar-brand navbar-b" href="{{ path('app_default_index') }}">
        <i class="fas fa-home"></i>
        Accueil
    </a>
    <ul class="navbar-nav gap-4">
        {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item dropdown" id="dropDownCompte">
                <a id="btnDropDownProjet" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" role="button">
                    <i class="fas fa-clipboard"></i> Projets
                </a>
                <div id="dropdownProjetAActiver" class="dropdown-menu" style="min-width: 5rem;">
                    <a class="dropdown-item" href="{{ path('app_projet_new') }}">Crée un nouveau projet</a>
                    <a class="dropdown-item" href="{{ path('app_projet_index') }}">Liste des projets (pub)</a>
                    <a class="dropdown-item " href="{{ path('app_projet_liste') }}">Liste des projets (admin)</a>
                </div>
            </li>
            <li class="nav-item dropdown" id="dropDownCompte">
                <a id="btnDropDownEditeur" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" role="button">
                    <i class="fas fa-language"></i> Langages
                </a>
                <div id="dropdownEditeurAActiver" class="dropdown-menu" style="min-width: 5rem;">
                    <a class="dropdown-item" href="{{ path('app_langage_new') }}">Crée un nouveau langage</a>
                    <a class="dropdown-item " href="{{ path('app_langage_index') }}">Liste des langages</a>
                </div>
            </li>
            <li class="nav-item dropdown" id="dropDownCompte">
                <a id="btnDropDownEditeur" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" role="button">
                    <i class="fas fa-file-lines"></i> Ide
                </a>
                <div id="dropdownEditeurAActiver" class="dropdown-menu" style="min-width: 5rem;">
                    <a class="dropdown-item" href="{{ path('app_ide_new') }}">Crée un nouveau IDE</a>
                    <a class="dropdown-item " href="{{ path('app_ide_index') }}">Liste des IDE</a>
                </div>
            </li>
        {% else %}
            <li class="nav-item">
                <a class=" nav-link" href="{{ path('app_projet_index') }}"><i class="fas fa-clipboard"></i> Projets</a>
            </li>
        {% endif %}
        <li class="nav-item">
            <a class=" nav-link" href="{{ path('app_default_contact') }}">Contact</a>
        </li>
        <li class="nav-item dropdown" id="dropDownCompte">
            <a id="btnDropDownEditeur" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" role="button">
                <i class="fas fa-user"></i> Compte
            </a>
            <div id="dropdownEditeurAActiver" class="dropdown-menu" style="min-width: 5rem;">
                <a class="dropdown-item{% if is_granted('ROLE_USER') %}" href="{{ path('app_usager_show', {'id': app.user.id}) }}"{% else %} disabled" href="#"{% endif %}>Mon Compte</a>
                {% if is_granted('ROLE_USER') %}
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Se Déconnecter</a>
                {% else %}
                    <a class="dropdown-item" href="{{ path('app_login') }}">Se Connecter</a>
                {% endif %}
            </div>
        </li>
        {#<li>
            <button class="btn" id="switch-mode" data-umami-event="Switch Theme">
                <img src="{{ asset('logo/light.png') }}" id="mode-dark" alt="light">
                <img src="{{ asset('logo/dark.png') }}" id="mode-light" alt="dark">
            </button>
        </li>#}
    </ul>
</nav>